how we open the terminal --- ctrl + `
step 1 --> initilize the project ; by using command npm init -y (-y defualt setting ke sath initilize kar deta humare app ko );
it will create a new file in our appliation thats name is package.json
it contain the meta data ;



create src folder  all work of the project take place in the src folder
now we create app.js file that is  the starting point of the application;
run file by using a command node app.js


server is a place where we listen the incoming request;
either we accept or reject the incoming request;


express js -> for creating server and api -> install by using a commmand npm i express
a new folder is created thats name is package.lock.json


node_modules->place jha pe sare dependencies aur nested dependencies ka code hota;
dependencies-> package on which our code aur appliation depend is dependencies;